{
  "name": "oogen-landing",
  "version": "1.0.0",
  "private": true,
  "description": "עוגן — landing page (developers). Content injected at build from content.json.",
  "scripts": {
    "build": "node build.js"
  },
  "dependencies": {
    "cheerio": "^1.0.0"
  }
}
